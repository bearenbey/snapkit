#!/bin/bash
set -eu

# Our gtkmm 4.12 shares the platform's soname, so it must come first.
export LD_LIBRARY_PATH="$SNAP/usr/lib/x86_64-linux-gnu${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"

exec "$SNAP/usr/bin/transmission-gtk" "$@"
